---
"api": minor
---

Add the Railway backend API (`apps/api`): a Hono server hosting Better Auth, the ElectricSQL shape auth-proxy, permission-gated write endpoints, public form intake, and employee invites — the self-hosted replacement for the Supabase backend.
